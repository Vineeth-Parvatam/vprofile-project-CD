apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "vprofile-helm.fullname" . }}-test-connection"
  labels:
    {{- include "vprofile-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "vprofile-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
