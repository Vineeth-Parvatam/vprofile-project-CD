apiVersion: v1
kind: Service
metadata:
  labels:
    app: {{ .Chart.Name }}{{ "-app" }}
  name: vproapp
  namespace: {{ .Values.namespace }}
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: {{ .Chart.Name }}{{ "-app" }}
  type: NodePort
