apiVersion: v1
kind: Service
metadata:
  creationTimestamp: null
  labels:
    app: {{ .Chart.Name }}{{ "-app" }}
  name: {{ .Chart.Name }}{{ "-app-svc" }}
  namespace: {{ .Values.namespace }}
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: {{ .Chart.Name }}{{ "-app" }}
  type: NodePort
status:
  loadBalancer: {}
