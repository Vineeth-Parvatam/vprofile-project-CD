apiVersion: v1
kind: Service
metadata:
  creationTimestamp: null
  labels:
    app: {{ .Chart.Name }}{{ "-rabbitmq" }}
  name: vpromq01
  namespace: {{ .Values.namespace }}
spec:
  ports:
  - port: 5672
    protocol: TCP
    targetPort: 5672
  selector:
    app: {{ .Chart.Name }}{{ "-rabbitmq" }}
status:
  loadBalancer: {}
